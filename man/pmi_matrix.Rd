% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmi_matrix.R
\name{pmi_matrix}
\alias{pmi_matrix}
\title{Create a Sparse PMI Matrix}
\usage{
pmi_matrix(pmi)
}
\arguments{
\item{pmi}{A data frame containing the PMI values with columns \code{code1}, \code{code2}, and \code{PMI}.
\code{code1} and \code{code2} represent the codes (words) being compared, and \code{PMI} is the
Pointwise Mutual Information between those codes.}
}
\value{
A symmetric matrix of class \code{dgCMatrix} where the rows and columns correspond to the unique
codes in the input data, and the values represent the PMI between the corresponding codes.
}
\description{
This function generates a sparse matrix representation of a Pointwise Mutual Information (PMI) matrix
based on input data, where each entry corresponds to the PMI between two codes (words).
It filters out zero values and creates a symmetric matrix.
}
\details{
The function uses the \code{Matrix} package to convert the sparse matrix. It ensures that the matrix
is symmetric by including both \verb{(i, j)} and \verb{(j, i)} pairs. Any PMI value of 0 is removed
before creating the final matrix.
}
\examples{
# Example input data frame
pmi <- data.frame(
  code1 = c("A", "B", "A", "C"),
  code2 = c("B", "C", "C", "A"),
  PMI = c(0.5, 0.2, 0.3, 0.6)
)

# Create the PMI matrix
pmi_matrix_result <- pmi_matrix(pmi)
# view the resulting sparse matrix
# pmi_matrix(pmi)
}
